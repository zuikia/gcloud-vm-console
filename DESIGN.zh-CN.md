# 设计登记

[English](DESIGN.md) · 简体中文

## 视觉方向

控制台采用克制的深色运维风格：中性深色背景、紧凑面板、清晰的文字层级、青色主操作、绿色成功、琥珀色警告和红色危险。界面应像实用的云控制台，而不是营销页面。

Latin UI 文本捆绑 IBM Plex Sans。中文无衬线和衬线文字使用操作系统的 `PingFang/Hiragino/Microsoft YaHei` 与 `Songti/STSong/SimSun` 字体栈，避免捆绑数 MB 的 CJK 字体。主画布保持稳定的纯色表面，不使用固定的径向涂层。

## 信息架构

侧栏有四个路由：

- 总览
- 实例
- 部署
- 任务

账号和项目选择位于总览中，不设置独立身份页，也没有全局实例选择器。

默认主题为深色。浅色主题的语义 token 保持并行维护，使未来切换主题不必重写组件级颜色。

## 布局规则

- D2 密度：正文 15-16px，标题紧凑，字段高度一致。
- 宽屏应通过 dashboard 网格、表格优先的资源布局和紧凑的执行摘要使用可用宽度。
- 1024px 及以下逐步减少列数。
- 390px 为单列布局，不允许按钮被裁切、文字竖排或水平溢出。
- 长项目 ID、IP、URL、节点链接和错误必须换行，或在自己的容器内滚动。
- 每次状态提交最多更新每个受影响根区域一次。搜索过滤只更新清单根节点，不得重渲染 Doctor、诊断或任务历史。
- 避免嵌套卡片和重复状态块。
- 桌面实例清单保持表格优先；900px 及以下改为带标签的分组行。
- 1080px 以上，选中实例状态摘要使用四列一行，配置/诊断/维护组共享一行。
- 768px 到 1080px，状态使用 2×2；配置与诊断共享第一行，维护跨下一行。
- 768px 以下状态为单列，原生动作折叠只打开推荐组。390px 下可见控件和摘要至少高 44px。
- 移动端以上的实例动作按钮使用紧凑双列网格；三动作组中可见动作和最后动作跨组宽度，避免不均匀分组产生过多空白。
- 技术详情、节点结果和危险操作使用原生 disclosure。长名称、ID、IP、链接、面板值、防火墙规则、恢复证据和错误保持在自己的容器中。
- 技术详情使用线性的三行概览：最终部署识别、问题数量与最高优先级问题，以及不可点击的恢复建议。展开的问题列表和恢复步骤不属于实例主层级。
- 机器、系统、来源、网络、身份、手动部署分类以及脱敏识别/验证证据位于一个次级 `更多属性` disclosure 中，实例选择改变时关闭。
- 部署页将公网 IPv4 模式、Network Tier 和 NIC 类型组成一个可见的紧凑网络配置行。条件式静态地址名称留在同一节中，可在下方换行而不创建卡片。实例页中这些内容仍是 `更多属性` 的次级属性，不得成为顶级状态卡片，也不得重复公网 IP 摘要。
- 细微光效只用于选中和状态，不使用装饰性光球、厚重玻璃效果或霓虹表面。

## 交互规则

- 点击行只选择实例。
- 详情面板根据选中状态提供一个建议。动作仍只归属于配置、诊断或维护之一；推荐行中唯一允许的本地 disclosure 动作是 `查看节点结果`。
- 头部显示实例名称、运行状态、公网 IP 和检查时间。部署方式由技术识别摘要负责。
- SSH、BBR、防火墙和服务状态各只出现一次作为规范状态卡。折叠证据可以显示端口、规则、服务名和错误，但不得创建第二个状态条。
- SSH 复制内容区分期望端口、已验证实际端口和回退。保留的 TCP 22 IAP 救援路径是支持证据，不是第二个 SSH 状态。
- 防火墙卡负责整体有效风险。即使匹配到控制台自有规则，只要项目级外部规则仍暴露选中实例，状态就不能显示为健康；自有规则覆盖率只能作为次级上下文。
- 配置负责 `设置部署 / 修改部署`、`基于此新建` 和适用的节点部署。诊断负责 `运行只读探测 / 重新探测` 和条件式 `接管本地`。维护只负责 `重启实例` 和 `系统更新`。
- 网络治理只有一个渐进披露的归属，即 `更多属性` 中的 `管理端口与 SSH`。紧凑对话框显示两个固定 SSH 入口、新鲜的可选监听器、本地密码配置状态和一个预览摘要；一次只显示一个主步骤。
- WARP 出口在 `节点结果` 中只有一个条件式归属：一行事实加一个紧凑对话框。对话框以 IPv4 为主，分别报告 IPv6/colo/协议，并只提供 `重新检测` 和一个动态确认动作（`重连并尝试换 IP`、`恢复 WARP 连接` 或 `再试一次`）。
- `只开实例` 不显示节点部署动作。防火墙状态仍是规范只读证据；治理详情只位于 `更多属性`，且只有新鲜、可执行的控制台自有计划才显示一个修复动作。
- 恢复指引只将最高优先级的现有动作作为文字，不在三个规范动作组之外创建第二个可点击入口或多步骤主列表。
- 当新鲜验证只是因为外部共享防火墙规则而处于 partial 时，主建议是人工复核，而不是再次探测或重新部署；任务结果不把它标记为可重试。
- 已有云实例部署使用与新建实例相同的表单。
- 保存草稿只写本地。
- 保存并预览会读取实时云状态并创建带指纹的预览。
- 编辑任何核心或高级配置字段都会使可执行预览失效。
- 改变公网 IPv4 模式、Network Tier、NIC 类型或静态地址名称都会使预览失效。没有公网 IPv4 时，节点部署在本地保存/预览前就会阻止；静态地址计费/保留风险同时显示在执行摘要和最终确认中。
- 只有存在有效预览时才启用执行。
- 维护按钮立即显示忙碌反馈，更新持久化任务条，并写入任务日志。
- 危险操作保持在底部的折叠区。
- 任务日志在任务页展开并置顶，但只存在于当前会话。持久化任务历史只读，选中行会更新进度、结果和节点链接面板。
- 1080px 以上，任务进度和预览组成紧凑的左侧上下文列，结果占据更宽的右列。诊断/证据和节点/防火墙产物默认关闭；平板和移动端将上下文与结果堆叠为一列。
- 重启恢复的任务使用一个琥珀色的 `interrupted` 终态和简洁说明，不显示为运行中、失败或可直接重试。除非需要恢复或隔离，否则隐藏存储维护提示。
- 每个终端任务阶段也必须是终态。启动兼容修复可以规范旧的 active 持久化阶段，但不得添加 UI 行或改变历史结果。
- 新鲜程度通过克制的色调和辅助文案表达，不增加状态卡。历史成功值不能保留 fresh-success 的呈现。
- `更多属性` 负责证据来源、证据时间、新鲜程度和最新尝试结果，同时承载基础/网络/身份属性与脱敏证据。
- 已验证的双入口策略把 SSH 保持为一个规范卡：`45400 · 双入口`，并用简洁文案说明 IAP 22 密钥加密码认证和公网 45400 密钥认证。有效实例隔离保持防火墙为一个规范卡，外部宽泛规则只作为项目级上下文报告。
- 页面加载和路由变化不会自动触发 SSH、清单、Guest Attributes 或防火墙探测。
- 选择实例或打开 WARP 对话框不发起网络请求。只有两个显式对话框动作可以调用 WARP API；取消重连不会改变对话框或远程实例。

## 组件语言

- 主按钮：云端写操作或下一步主动作。
- 次按钮：导航、刷新、只读检查和非主动作。
- 危险按钮：移除本地记录或删除云端资源。
- 徽章描述来源和状态，不使用营销式表述。
- 日志默认是固定高度终端区域中的简短状态行；原始错误和长链接在各自容器内滚动。
- 用户可见的任务阶段详情和节点防火墙动作使用中文标签；稳定的协议名、端口、规则名和 gcloud 命令名保留技术原文。
- 即时反馈属于 toast，持久化结果属于任务历史，当前会话详情属于运行日志；这些通道不得重复完整结果块。
- 缓存清单使用低密度行内提示，不新建面板。禁用的云端动作说明需要恢复实时清单；本地动作保持原有层级。

## 验证规则

- `npm --prefix ui run check` 是静态和单元门禁。
- `UI_AUDIT_SCREENSHOTS=1 npm --prefix ui run audit:layout` 是覆盖 1728、1440、1280、1024、768 和 390 宽度的响应式布局门禁。
- 布局审计被浏览器阻塞时必须明确失败，不能伪装成成功。
- 自动化验证不得确认会改变云端的对话框，也不得执行真实 GCP 写操作。
- 实时验收 CLI 只允许实例 describe/list、Guest Attributes 读取、防火墙 list/describe，以及通过原生 `ssh` 加 `gcloud compute start-iap-tunnel` 执行的固定 SSH 探测。`gcloud compute ssh` 会被拒绝，因为它可能写入项目 SSH 元数据。CLI 会比较探测前后的记录、任务历史和节点结果哈希。
- `npm --prefix ui run audit:performance:browser` 必须报告最多四个启动波次、无重复/非只读启动请求，以及受限的 DOM mutation 数量。
- `npm --prefix ui run cleanup:local` 默认是 dry-run；只有 `--apply` 才能移除经过测试的缓存允许列表。
