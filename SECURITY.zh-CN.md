# 安全政策

[English](SECURITY.md) · 简体中文

## 范围

GCP VM Console 是一个本地运维控制台。它可以调用 `gcloud`、SSH，以及用户提供的启动或部署脚本，对操作者选择的基础设施执行操作。

只应在你拥有或获授权管理的 Google Cloud 项目、VM、网络和主机上使用本项目。不要用本项目扫描、探测或修改第三方基础设施。

## 报告漏洞

请不要在公开 issue 中提交凭据、私钥、真实节点链接、与私有部署关联的项目标识或公网 IP，也不要提交会改变云资源的复现步骤。

在项目公布私密联系方式之前，请通过私有 GitHub Security Advisory 或直接联系维护者报告疑似问题。请提供受影响版本、尽可能使用 mock 数据的最小复现，以及影响说明，并在公开披露前留出修复时间。

## 凭据和数据规则

- 永远不要提交 gcloud 凭据、服务账号密钥、SSH 私钥、token、密码、节点链接或本地运行时状态。
- 将 `.gcp-vm-console/` 保持在版本控制之外。
- 测试和示例使用假项目、文档专用保留 IP 段和 mock runner。
- 分享前检查生成的日志、截图和任务记录。
- 云端写操作应继续受到应用的预览、指纹、确认和任务锁边界保护。

## 重要的本机信任边界限制

- loopback API 不提供用户身份认证。同一用户运行的任何本机进程都可能调用它；浏览器确认是安全 UX 边界，不是授权边界。请保持 8787 只绑定本机 loopback，并保护运行控制台的电脑。
- 本地 Secret Store 依靠受限文件权限保护（通常为 `0700` 目录和 `0600` 文件），不是加密保险箱。请同时保护备份、同步目录和本机账号。
- 只读 SSH 探测目前不会持久化主机密钥校验。请使用可信网络，并通过独立渠道核对报告的指纹后再依赖探测结果。
- 可选的 Sing-Box-Plus 和 3X-UI 适配器会获取上游安装脚本并以 `root` 执行。请将其视为实验功能，使用前审查脚本和上游版本；生产主机还应配合你自己的供应链控制。
- `gcloud compute ssh` 在远程写操作前可能创建或刷新 Google Cloud SSH metadata。启用 SSH 操作时，请检查预览以及项目/实例 metadata 的变化。
- 自定义启动脚本文本会被写入本地记录、API 响应和 Compute Engine metadata。脚本中永远不要放密码、token 或私钥。

3X-UI 安装器凭据只用于记录“凭据已生成”；当前版本不会把密码或 API token 持久化到 VM 记录、任务结果或浏览器界面。旧记录在读取或保存时会被清理。
