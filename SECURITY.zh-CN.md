# 安全政策

[English](SECURITY.md) · 简体中文

## 范围

GCP VM Console 是一个本地运维控制台。它可以调用 `gcloud`、SSH，以及用户提供的启动或部署脚本，对操作者选择的基础设施执行操作。

只应在你拥有或获授权管理的 Google Cloud 项目、VM、网络和主机上使用本项目。不要用本项目扫描、探测或修改第三方基础设施。

## 报告漏洞

请不要在公开 issue 中提交凭据、私钥、真实节点链接、与私有部署关联的项目标识或公网 IP，也不要提交会改变云资源的复现步骤。

在项目公布私密联系方式之前，请通过私有 GitHub Security Advisory 或直接联系维护者报告疑似问题。请提供受影响版本、尽可能使用 mock 数据的最小复现，以及影响说明，并在公开披露前留出修复时间。

## 凭据和数据规则

- 永远不要提交 gcloud 凭据、服务账号密钥、SSH 私钥、token、密码、节点链接或本地运行时状态。
- 将 `.gcp-vm-console/` 保持在版本控制之外。
- 测试和示例使用假项目、文档专用保留 IP 段和 mock runner。
- 分享前检查生成的日志、截图和任务记录。
- 云端写操作应继续受到应用的预览、指纹、确认和任务锁边界保护。
