# 产品登记

[English](PRODUCT.md) · 简体中文

## 产品

GCP VM Console 是一个面向小型 Google Cloud Compute Engine 实例的本地运维控制台。它以 gcloud 作为唯一云端控制路径，在一个界面中处理账号/项目选择、实例清单、部署预览、维护任务、节点部署、日志和结果。

## 用户

主要用户是希望创建或维护小型实例、但不想记忆 gcloud、SSH、IAP、防火墙和节点部署命令的初级或中级 Google Cloud 用户。

## 产品原则

1. 一个控制模型：所有云端操作都通过 gcloud，并明确配置、账号、项目和位置。
2. 一个清单：本地记录和云端实例出现在同一实例列表中。
3. 一个决策点：部署写操作必须拥有当前预览和指纹。
4. 每个上下文只有一个主动作：每个页面都应明确下一步安全操作。
5. 本地删除和云端删除保持分离。
6. 节点部署是自动化流水线；只有失败后才显示阶段重试。
7. 旧工作流文件只作为隔离的迁移证据保留。
8. 读路径要协调并去重；性能改动不能削弱确认、指纹、任务锁或本地持久化边界。

## 页面

- 总览：账号/项目上下文、资源摘要、当前实例、最近任务、下一步动作和折叠的费用提示。
- 实例：统一实例清单、规范运行状态、一个推荐下一步、三个互不重叠的动作组，以及渐进披露的技术/节点/危险详情。
- 部署：新建、编辑和基于现有实例新建共用一套表单；核心字段可见，VPC/SSH 高级字段折叠，并显示执行摘要。没有有效的历史区域时，新草稿默认 `us-west1`；明确选择的有效值会保留。
- 任务：日志优先的任务工作区、只读任务历史、选中任务进度、执行摘要，以及渐进披露的诊断、节点链接、防火墙详情和原始错误。

## 当前界面契约

- 账号/项目切换先进入待定状态，只有项目和资源同步成功后才提交。
- 桌面实例清单严格包含五列：实例、位置、状态、公网 IPv4 和模式。账号、项目、机器、系统和网络详情位于选中实例的检查器中。
- 选中实例头部只负责名称、运行状态、公网 IP 和最近检查时间；部署方式不在此重复显示。
- 选中实例检查器为 SSH、BBR、防火墙和服务状态各提供一个规范可见归属。证据优先级依次为最新验证、当前识别探测、节点结果、已观察 SSH、待定状态。
- 部署方式只显示一个最终识别类型或 `未识别`；识别冲突不会成为主界面状态。
- 实例动作分为三个互不重叠的归属：配置（`设置部署 / 修改部署`、克隆、适用的节点部署）、诊断（只读探测、条件式本地接管、条件式控制台自有防火墙修复）和维护（重启、系统更新）。
- `vm_only` 隐藏节点部署。防火墙修复只在治理详情内保留一个条件式归属；不存在重复的独立防火墙按钮。
- 技术详情只先展开三行摘要：部署识别、验证问题摘要和一个恢复建议。完整基础/网络/身份属性、手动分类和脱敏证据放在默认关闭的 `更多属性` 中。
- 节点链接或 3X-UI 面板数据、节点防火墙规则详情和危险操作通过独立折叠区域提供，不重复四个规范状态。
- 符合条件的 Sing-Box-Plus 记录在 `节点结果` 中显示一个紧凑 WARP 出口行。对话框负责显式刷新状态和一次确认后的重连；它不会成为第五个规范状态或维护动作。
- 部署页用一个紧凑可见的网络配置区展示公网 IPv4 模式、Compute Engine Network Tier 和 NIC 类型；VPC/子网、SSH、标签、网络标签和启动脚本放在一个高级折叠区。启动脚本和三项网络选择都会参与目标状态哈希。
- 新草稿默认 `static + PREMIUM + GVNIC`。静态模式会推导可编辑的区域地址资源名，显示未使用地址的计费风险，并把地址预留作为明确的预览动作。仍可选择 `ephemeral` 和 `none`；关闭公网 IPv4 时，Sing-Box-Plus 和 3X-UI 会失败关闭。
- 静态地址执行继续受现有指纹、确认、持久化 Job 和任务锁保护。只复用区域和 Network Tier 匹配的已预留地址；部分失败后不会自动删除地址，并明确报告保留的资源。
- 选择自定义 SSH 端口时，通过仅限 IAP 的控制台自有规则保留 TCP 22 作为救援路径。只有该端口连接成功后才会把自定义端口报告为实际端口；回退过程保持显式。
- 任务页顶部始终显示任务日志，并且日志只存在于当前会话。结构化任务历史随后显示，保存在本地 `.gcp-vm-console/jobs`，通过 `GET /api/jobs` 读取；没有任务管理写接口。
- 选中任务摘要和恢复指引保持可见。诊断/证据和节点/防火墙产物默认折叠，进度提示跟随选中任务类型，不重复预览提示。
- 终端任务最多保留 30 天、200 条。服务重启时仍处于 queued 或 running 的任务会变为 `interrupted`；不会自动续跑，必须通过现有只读诊断流程确认。
- 旧终端任务若保存的阶段仍为 active，会在启动时进行本地兼容修复，避免已完成任务再次显示为运行中。该修复不会发出云端或 SSH 请求。
- 确认策略集中管理：只读动作直接执行，云端写操作需要一次确认，本地危险动作需要输入确认文字。
- 运行时证据按字段独立处理。SSH、BBR、防火墙、服务和部署方式分别使用 `fresh <= 15m`、`aging <= 6h`、`stale > 6h` 或时间无效时的 `unknown`。
- 更新的成功只读探测可以替换过期验证证据；过期探测不能替换用户确认或持久化的部署意图。
- 最新探测失败会单独保存为 `observed.lastProbeAttempt`，不会抹掉最后可用的验证；界面会把保留值标记为历史值。
- 只读 SSH 诊断使用固定探测命令，通过原生 `ssh` 和 `gcloud compute start-iap-tunnel` ProxyCommand 执行。它不会调用 `gcloud compute ssh`，因为后者即使远程命令只读，也可能写入项目 SSH 元数据。
- 在没有持久化用户/本地意图时，部署识别选择最高置信度。高置信度的实时 SSH 深度探测会优先于匹配的中置信度云标签，不会人为保持在中置信度。
- 实时验收是独立的、允许列表内的、不持久化的读流程，目标为 `example-singbox-instance` 和 `example-xui-instance`。它不是修复或部署流程。
- 初始加载限制为四个请求波次：health、jobs/accounts、projects，然后一次并行上下文快照。较早的项目响应不能覆盖当前上下文。
- `刷新数据` 负责完整上下文快照：记录、云端清单、区域、免费规则和 Doctor。`刷新清单` 只负责记录、云端清单和派生的 Doctor 结果。
- 静态 HTML、JavaScript 和 CSS 使用 ETag/Last-Modified 重新验证以及 Brotli/gzip。界面只捆绑 IBM Plex Sans，中文使用平台 CJK 字体栈。
- `/api/health` 暴露确定性的运行时源版本。本地启动脚本在复用 8787 端口前比较该版本，因此活动服务端或前端源变化后，即使旧进程健康也会重启。
- HTTP body 限制为 1 MiB。gcloud 命令使用分类超时和 4 MiB 标准输出/错误输出上限；超时、输出溢出、启动失败和命令失败保持为不同的脱敏错误。
- 普通 gcloud 读操作只对分类的临时网络/代理失败重试，最多三次，等待 500 ms 和 1500 ms。写操作、SSH、认证、权限、未找到、超时和输出上限失败都不会重试。
- 实例清单按 gcloud 配置/账号/项目原子缓存，最多七天、二十个上下文。缓存模式只读：浏览、搜索、诊断、本地草稿、复制、任务历史和本地记录移除仍可用，但所有依赖云端的写操作会锁定。
- 智能诊断生成一个项目级防火墙治理评估。每条规则记录严重级别、归属、源/目标范围和受影响实例；外部规则只提供建议。高或严重级别的外部规则会使验证保持 `partial`，直到实例级隔离结果证明选中 VM 不再广泛暴露。
- `管理端口与 SSH` 是唯一的网络治理入口。它使用新鲜监听证据构建 15 分钟指纹化预览，通过密钥加密码认证在 IAP 上保留 TCP 22，通过密钥认证公开 TCP 45400，并且只应用控制台自有的允许/拒绝规则和一个唯一实例标签。
- WARP 管理只支持实时确认的 `warp-svc + WarpProxy + Sing-box SOCKS outbound` 部署。重连可能返回 `changed`、`unchanged`、`restored`、`partial` 或 `failed`；IPv4 未变化仍是成功的一次尝试，不会自动进行第二次重连。
- WARP 状态和重连在已验证的密钥认证端口上使用固定原生 SSH 契约。它们不会重启 VM、Sing-box 或 `warp-svc`，也不会改变防火墙规则、端口、节点链接、配置、密钥、注册状态或隧道协议。
- SSH 密码只在首次使用时接收，并存储在本地 `0700`/`0600` Secret Store 中。密码值不会进入 VM 记录、任务、日志、诊断、API 响应、命令参数或云端可见字段。
- 对托管节点，已识别的节点监听器默认选中；未知进程监听器保持关闭，直到用户明确选择。深度探测证据不能悄悄扩大可执行的网络暴露计划。
- 本地缓存清理是显式 CLI 维护动作，默认 dry-run，只删除允许列表中的生成缓存，不会涉及 VM 记录、任务、节点结果、凭据、Playwright 浏览器或旧归档。
- 权威文档可以记录端口和脱敏结果，但不得保留原始节点链接、实时面板 URL、明文凭据或私钥路径。

## 非目标

- 不提供托管式多用户控制平面。
- 迁移期间不自动删除云资源。
- 当前版本不提供负载均衡器、域名、HTTPS 证书或 Cloudflare 自动化。
- 不在实例流程之外进行广泛的 Google Cloud 资源编排。
- 不自动禁用、更新或删除外部/共享防火墙规则，例如 `ruzhan1` 或 `default-*`。
